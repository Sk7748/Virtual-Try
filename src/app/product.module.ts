export interface Product {
    id: number;
    name: string;
    description: string;
    imageUrl: string;
    category: string; 
    dimage:string// Add category property
    // Add any other properties as needed
  }
  